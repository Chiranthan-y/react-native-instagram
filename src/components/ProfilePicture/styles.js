import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    margin: 5,
    borderRadius: 40,
    borderWidth: 3,
    borderColor: '#AE22E0',
  },
  image: {
    borderRadius: 40,
    borderWidth: 2,
    borderColor: '#FFFFFF',
  },
});

export default styles;
